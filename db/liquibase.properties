# Liquibase properties — used when running liquibase CLI directly (outside Docker)
# The Docker Compose liquibase service passes these as CLI args instead.

url=jdbc:mysql://localhost:3306/nexus?useSSL=false&allowPublicKeyRetrieval=true
username=${MYSQL_USER}
password=${MYSQL_PASSWORD}
changeLogFile=changelogs/db.changelog-master.xml
logLevel=INFO
